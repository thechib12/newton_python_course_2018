# Import the library and give it a smaller name
import numpy as np

# Define an array
a = np.array([1, 2, 3, 6])

# Define a 4x3 matrix
b = np.array([[1, 2, 3], [3, 4, 6.7], [5, 9.0, 5], [2, 3, 6]])

# Prints the array size (row,)
print(a.shape)

# Prints the matrix shape with (row, column)
print(b.shape)